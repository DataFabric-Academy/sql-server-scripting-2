/*
================================================================================
  Lab 04 — exercise.sql  (ผู้เรียน)
  Concurrency
================================================================================
  ข้อ 1–2 ทำในหน้าต่างเดียวได้
  ข้อ 3–4 ต้องเปิด 2 sessions (หรืออธิบายลำดับ step ให้ชัดในคอมเมนต์)
================================================================================
*/

USE AdventureWorks;
GO

SET NOCOUNT ON;
GO

/*------------------------------------------------------------------------------
  TODO 1 — อ่านค่า isolation / snapshot flags ของ database ปัจจุบัน
  SELECT จาก sys.databases แสดง:
    name, snapshot_isolation_state_desc, is_read_committed_snapshot_on
------------------------------------------------------------------------------*/
-- TODO 1: WRITE YOUR CODE HERE


GO

/*------------------------------------------------------------------------------
  TODO 2 — ตารางสรุป (เป็นคอมเมนต์หรือ SELECT จำลองก็ได้)
  สร้างผลลัพธ์ 4 แถว (isolation level) พร้อมคอลัมน์
    AllowsDirtyRead, AllowsNonRepeatable, AllowsPhantom
  เป็น 'Y'/'N' ตามทฤษฎีสำหรับ:
    READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, SERIALIZABLE
------------------------------------------------------------------------------*/
-- TODO 2: WRITE YOUR CODE HERE


GO

/*------------------------------------------------------------------------------
  TODO 3 — Dirty read ด้วยตัวเอง (2 sessions)
  เขียนคอมเมนต์ลำดับ Step ในไฟล์นี้ให้ครบ:
    Session A ทำอะไร / Session B ทำอะไร
  และในหน้าต่างของคุณ ลองทำให้ SELECT ด้วย READ UNCOMMITTED
  เห็น PhoneNumber ของ BusinessEntityID = 105 ที่อีกฝั่ง UPDATE แล้วยังไม่ commit
  จากนั้นอีกฝั่ง ROLLBACK
  *** ห้าม COMMIT ค่าทดลอง ***
------------------------------------------------------------------------------*/
-- TODO 3: ใส่ลำดับ Step เป็นคอมเมนต์ + (ถ้าสะดวก) วางสคริปต์ Session A ที่นี่


GO

/*------------------------------------------------------------------------------
  TODO 4 — เลือก isolation ให้เคสสต็อก
  สมมติ: Session อ่าน Quantity ของ Product 854 แล้วค่อย UPDATE ตัดสต็อก
  อธิบายด้วย PRINT ว่าทำไม READ COMMITTED อย่างเดียวอาจไม่พอ
  และเสนออย่างน้อย 1 วิธี (เช่น REPEATABLE READ / UPDLOCK / SNAPSHOT + retry)
------------------------------------------------------------------------------*/
-- TODO 4: WRITE YOUR CODE HERE


GO

/*------------------------------------------------------------------------------
  TODO 5 — ปัญหาบนสไลด์ 56 ที่ lab session เดิมยังไม่โชว์ครบ
  เขียน PRINT อธิบายสั้น ๆ (2–4 บรรทัดต่อข้อ):
    5.1 Lost update — เกิดเมื่อไร / กันอย่างไรอย่างน้อย 1 วิธี
    5.2 Double read — ต่างจาก Phantom อย่างไร
  (ไม่บังคับรัน 2 session — เน้นความเข้าใจตาม PPT)
------------------------------------------------------------------------------*/
-- TODO 5: WRITE YOUR CODE HERE


GO

/*------------------------------------------------------------------------------
  TODO 6 — แพทเทิร์นสไลด์ 59 (เขียนโครงเปล่าให้ครบ)
  ใน comment อธิบายความต่าง:
    A) SET TRANSACTION ISOLATION LEVEL SERIALIZABLE ทั้ง session
    B) ใช้ WITH (SERIALIZABLE) เฉพาะบน MiniOrderDetails / MiniProducts
  แล้วเขียนโครง TRY/CATCH + XACT_ABORT แบบย่อ (ไม่ต้อง INSERT จริงก็ได้)
  ที่แสดงทั้งสองแนวทาง
------------------------------------------------------------------------------*/
-- TODO 6: WRITE YOUR CODE HERE


GO

PRINT N'===== Lab 04 exercise: ส่งงานเมื่อครบ TODO =====';
GO
